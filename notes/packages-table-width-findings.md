Packages table inspection at the current 1280px preview viewport showed the table still using the base auto layout with a 920px minimum width. The first body cell computed to a 300px minimum and approximately 519.7px rendered width, which explains the oversized empty area and mobile expansion. The mobile media rules need to override the base `td:first-child` minimum and force a compact fixed layout at the relevant breakpoint.
