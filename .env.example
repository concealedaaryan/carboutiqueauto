# Server-only secret used by api/chat.js for the Groq GPT-OSS 20B chatbot.
# Add the real value in Vercel Project Settings → Environment Variables.
# Never rename this to PUBLIC_GROQ_API_KEY and never commit a real secret.
GROQ_API_KEY=

# Public HTTPS domain used for canonical tags, social metadata, JSON-LD, and sitemap generation.
# Set this explicitly to the real custom production domain when available.
# On Vercel, the build can fall back to VERCEL_PROJECT_PRODUCTION_URL when
# Vercel system environment variables are enabled for the project.
PUBLIC_SITE_URL=https://your-domain.example

# Keep legal-policy drafts out of search until approved and finalized.
PUBLIC_INDEX_LEGAL=false
