# Image-backed section browser audit

## Homepage

The homepage renders the Crest + Rodim PPF hero with the heading `Protection with precision.`, the primary `Explore Rodim PPF` CTA, and the three-slide background-image carousel. The dark gradient overlay keeps the white and silver hero copy readable over the studio photography. Previous/next controls and the three slide indicators are visible.

## Protection route

The Protection page renders the Rodim PPF hero with a photographic precision-installation background, a readable overlay, a `Build a Rodim PPF estimate` CTA, the Rodim-only PPF pricing matrix, and image-backed section treatment continuing below the hero.

## Automated checks

`pnpm check` passed with 0 errors, 0 warnings, and 0 hints.

`pnpm build` passed and generated 17 static routes.

`pnpm test:e2e` passed all 17 tests.
